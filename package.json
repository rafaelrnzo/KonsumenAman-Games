{
  "name": "fekdi-party-games",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/write-config.mjs && npm test",
    "test": "node --test tests/*.test.mjs"
  }
}
